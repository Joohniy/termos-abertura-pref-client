export const successMessage = (message) => {
    return <p className="FieldSuccess">{message}</p>;
  };
export const errorMessage = (message) => {
    return <p className="FieldError">{message}</p>;
  };
export const commonMessage = (message) => {
    return <p className="FieldCommon">{message}</p>;
  };
